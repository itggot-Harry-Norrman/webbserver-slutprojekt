<!DOCTYPE html>
html lang="en"
    head
        meta charset="UTF-8"/
        meta name="viewport" content="width=device-width, initial-scale=1.0"/
        meta http-equiv="X-UA-Compatible" content="ie=edge"/
        link rel="stylesheet" href="css/style.css"/
        title Document
    body
        .wrapper
            ==yield
    script src="js/jquery-3.2.1.min.js"
    javascript:
            $("#hide").ready(function(){
                $(".add_post").hide();
            });
            $("#hide").click(function(){
                $(".add_post").toggle(400);
            });
            $('.plus').click(function(){
                $(this).siblings(this).toggleClass("hide_this")
                $(this).toggleClass("rotate_this")
            });